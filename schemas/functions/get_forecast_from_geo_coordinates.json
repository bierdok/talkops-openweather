{
  "name": "get_forecast_from_geo_coordinates",
  "description": "Get weather forecast for 5 days with data every 3 hours from geo coordinates. Don't provide scientific data except for temperature and wind unless specifically requested.",
  "parameters": {
    "type": "object",
    "properties": {
      "latitude": {
        "type": "string",
        "description": "The latitude of the location."
      },
      "longitude": {
        "type": "string",
        "description": "The longitude of the location."
      }
    },
    "required": ["latitude", "longitude"]
  }
}
